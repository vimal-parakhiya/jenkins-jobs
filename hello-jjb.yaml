- job:
    name: Basic example
    project-type: freestyle
    description: 'A basic example of a `jjb` job that echos "Hello JJB!!"'
    builders:
        - shell: |
            echo 'Hello Jenkins Job Builder!'
